# 2026-06-14

## What I Learned

- Day 6 tập trung vào FastAPI structure: router, service layer, dependency, Pydantic schemas và global error handlers.
- Route handler nên mỏng, business logic nên nằm trong `services/`.
- `Depends()` hữu ích cho logic dùng lại như pagination, sau này là DB session hoặc current user.
- Request model và response model nên tách riêng để tránh leak field nội bộ như `password`.

## What I Built

- Refactor `journal/fastapi-hello-world` từ `app/schemas.py` thành package `app/schemas/`.
- Thêm `app/services/` với `echo_service.py` và `user_service.py`.
- Thêm CRUD `/users` in-memory:
  - `POST /users`
  - `GET /users`
  - `GET /users/{user_id}`
  - `PUT /users/{user_id}`
  - `PATCH /users/{user_id}`
  - `DELETE /users/{user_id}`
- Thêm `pagination` dependency trong `app/deps.py`.
- Thêm global error envelope trong `app/error_handlers.py`.
- Thêm tests cho users CRUD, duplicate email, pagination, not found và validation error.

## Verification

```bash
cd journal/fastapi-hello-world
uv run pytest
uv run ruff check .
```

Kết quả:

- `14 passed`
- `ruff`: `All checks passed!`

## Blockers

- Không có blocker lớn.
- Có warning từ `fastapi.testclient`/Starlette về `httpx`, nhưng tests vẫn pass và warning đến từ dependency.

## Question

- Khi chuyển từ in-memory users sang SQLAlchemy ở Day 8, nên giữ service interface hiện tại như thế nào để route không phải đổi nhiều?
