# 2026-06-28

## What I Learned

- Day 12 tập trung vào structured logging, correlation ID và cách đọc log khi request lỗi.
- `structlog.contextvars` giúp mọi log trong cùng request tự mang `correlation_id`.
- Global exception handler cần log stack trace bằng `logger.exception`, còn response cho user vẫn nên sạch và ổn định.

## What I Built

- Thêm `structlog` và cấu hình JSON logs cho môi trường non-dev.
- Thêm `CorrelationIdMiddleware` để nhận hoặc tạo `X-Request-ID`.
- Chuyển request log và auth login log sang structured events.
- Thêm unhandled exception handler trả error envelope chuẩn.
- Thêm tests cho request ID và log khi endpoint lỗi 500.
- Mask email trong log `login_failed` bằng `email_hash` và `email_domain` để giảm rủi ro PII.

## Verification

```bash
cd journal/fastapi-hello-world
uv run pytest
uv run ruff check .
```

Kết quả Day 12:

- `pytest`: `46 passed`, có 2 warnings từ dependencies/test client.
- `ruff`: `All checks passed!`

## Grep Drill

Các lệnh kiểm tra log cần chạy khi app ghi log ra file:

```bash
rg '"correlation_id":"err-id-123"' /tmp/day12-api.log
rg '"level":"(warning|error|critical)"' /tmp/day12-api.log
rg '"event":"login_failed".*"reason":"invalid_credentials"' /tmp/day12-api.log
```

Ý nghĩa:

- Lệnh 1 gom toàn bộ log của một request cụ thể.
- Lệnh 2 tìm các log từ `WARNING` trở lên để ưu tiên điều tra.
- Lệnh 3 lọc login failure theo event và reason, không cần grep email hoặc password.

## Metrics Sketch

- `http_requests_total` counter, labels `method`, `path`, `status`; alert khi tỉ lệ `5xx` lớn hơn 5% trong 5 phút.
- `http_request_duration_seconds` histogram, labels `method`, `path`; alert khi p95 của endpoint chính lớn hơn 500ms trong 10 phút.
- `auth_login_failures_total` counter, labels `reason`, `email_domain`; alert khi một domain hoặc toàn hệ thống tăng bất thường trong 5 phút.
- `db_query_duration_seconds` histogram, labels `operation`, `table`; alert khi p95 query đọc/ghi vượt 300ms trong 10 phút.
- `unhandled_exceptions_total` counter, labels `exception_type`, `path`; alert ngay khi có hơn 3 lỗi mới trong 5 phút.

## Blockers

- Chưa có blocker.

## Question

- Email hash trong log đã đủ để debug login failure chưa, hay nên thêm user-safe identifier khác?
